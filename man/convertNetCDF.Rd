% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convertNetCDF.R
\name{convertNetCDF}
\alias{convertNetCDF}
\title{Converts odf object to netCDF}
\usage{
convertNetCDF(odf, filename = NULL, debug = 0, data = NULL, destination = ".")
}
\arguments{
\item{odf}{an odf object from oce which contains mctd or rcm data}

\item{filename}{the desired name for the netCDF file produced (not including the extension), if left NULL
the default will conform to Bedford Institute of Oceanography ("BIO") naming conventions}

\item{debug}{integer value indicating level of debugging.
If this is less than 1, no debugging is done. Otherwise,
some functions will print debugging information.}

\item{data}{a data frame of standard name, name, units, and GF3 codes likely from \code{\link[=getStandardData]{getStandardData()}}}

\item{destination}{the specified location to save the NetCDF. By default this is set
to the local directory}
}
\value{
netCDF file with a maximum of 12 variables
}
\description{
This function converts an odf object to a NetCDF file for CTD
and RCM types.
}
\examples{
\dontrun{
library(odfToNetCDF)
library(oce)
data <- getStandardData(type="ctd")
f <- system.file("extdata", "mctd.ODF", package="odfToNetCDF")
odf1 <- read.odf(f)
odf2 <- nameReplacement(odf1, data=data, unit="S/m")
odf3 <- removeDerived(odf2)
odf4 <- fixMetadata(odf3, data=data)
convertNetCDF(odf4, data=data)
}
}
